{
  "name": "knex-migrations",
  "version": "1.0.0",
  "description": "require('dotenv').config();",
  "main": "knexfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "knex": "^2.3.0"
  }
}
